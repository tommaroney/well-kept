spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wellkept_db?useSSL=false
    username: root
    password: root
    initialization-mode: always
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
  data:
    jpa:
      repositories:
        enabled: true